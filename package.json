{
  "name": "marathon-skills-web",
  "version": "2.0.0",
  "private": true,
  "description": "Marathon Skills web client with Vercel API, Firebase Google Auth and Supabase storage.",
  "scripts": {
    "check": "node --check app.js && node --check auth.js && node --check api/config.js && node --check api/participants.js && node --check api/admin-tasks.js && node --check api/events.js && node --check api/telegram-webhook.js && node --check api/_lib/telegram.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  }
}
